# Gemini API Key — required for quiz generation (Free tier from Google AI Studio)
GEMINI_API_KEY=your_key_here

# Optional: Override the default model
# GEMINI_MODEL=gemini-flash-lite-latest

# Optional: Server config
# HOST=0.0.0.0
# PORT=8000
